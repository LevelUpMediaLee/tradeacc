# Bots

Every folder here is one self-contained Vercel Project. A bot owns its HTML,
assets, API functions, prompt, environment-variable example and Vercel settings.

Use a separate Vercel Project for each bot so domains, logs, keys and rollbacks
remain independent.
