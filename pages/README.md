# Landing pages and static sites

Every folder here is one self-contained static Vercel Project. Keep the page's
HTML and assets inside its own folder, then choose that folder as the Root
Directory when importing the repository into Vercel.

Static sites need no API keys unless their own functionality calls a service.
